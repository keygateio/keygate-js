{
  "name": "keygate-js",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "turbo run build  --include-dependencies",
    "generate": "pnpm run --filter api generate",
    "client": "pnpm run --filter client dev",
    "ui": "pnpm run --filter ui dev"
  },
  "devDependencies": {
    "rome": "11.0.0",
    "turbo": "^1.6.3"
  }
}
