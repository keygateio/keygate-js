{
  "name": "@keygate/core",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts src/worker.ts --bundle --minify --platform=neutral --target=es2020 --outdir=lib"
  },
  "dependencies": {
    "node-fetch": "^3.2.10"
  },
  "devDependencies": {
    "idb-keyval": "^6.2.0",
    "esbuild": "^0.15.11"
  }
}
