{
  "name": "@keygate/core",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --platform=neutral --target=es2022 --outdir=lib"
  },
  "dependencies": {
    "node-fetch": "^3.2.10"
  },
  "devDependencies": {
    "esbuild": "^0.15.11",
    "idb-keyval": "^6.2.0"
  }
}
